// import { environment } from '../../../../src/environments/environment';

/// Don't use underscore variable for links
/// Always end the links with trailing slash;

export const UrlConfig = {
  totalUrl:

  {
      UserSignUps: `api/v1/UserSignUps`
  }
};
